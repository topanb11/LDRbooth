{
  "name": "ldrbooth-client",
  "version": "1.0.0",
  "private": true,
  "description": "LDRbooth frontend (TypeScript, no framework).",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
